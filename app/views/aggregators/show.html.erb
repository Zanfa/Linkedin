<p id="notice"><%= notice %></p>

<h1><%= @aggregator.name %></h1>

<p>
  <strong>Invite key:</strong>
  <%= link_to invite_url(@aggregator.invite_key), invite_url(@aggregator.invite_key) %>
</p>

<h2>Profiles</h2>
