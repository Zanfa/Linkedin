<%= form_for(@aggregator) do |f| %>
  <% if @aggregator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aggregator.errors.count, "error") %> prohibited this aggregator from being saved:</h2>

      <ul>
      <% @aggregator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
